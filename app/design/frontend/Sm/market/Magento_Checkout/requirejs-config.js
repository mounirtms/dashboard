/**
 * RequireJS configuration for checkout customizations
 * Note: Mixin removed - using component replacement instead for better compatibility
 */
var config = {
    // Component replacement approach - no mixins needed
    // The discount component is replaced via layout XML with our disabled version
};
